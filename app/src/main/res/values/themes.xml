<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TechnfestCRM" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.TechnfestCRM" parent="Base.Theme.TechnfestCRM" />



    <style name="ToolbarTitleText" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textSize">18sp</item> <!-- Change title size -->
        <item name="android:textStyle">bold</item>
    </style>

    <style name="ToolbarSubtitleText" parent="TextAppearance.Widget.AppCompat.Toolbar.Subtitle">
        <item name="android:textSize">12sp</item> <!-- Change subtitle size -->
    </style>

    <style name="CustomToolbarStyle" parent="Widget.MaterialComponents.Toolbar">
        <item name="android:textColorPrimary">@android:color/black</item>
        <item name="android:textColorSecondary">@android:color/black</item>
        <item name="titleTextColor">@android:color/black</item>
        <item name="subtitleTextColor">@android:color/black</item>
        <item name="itemIconTint">@android:color/black</item>
        <item name="itemTextColor">@android:color/black</item>
    </style>

    <style name="MenuPopupLight" parent="ThemeOverlay.MaterialComponents.Light">
        <item name="android:textColor">@android:color/black</item>
        <item name="android:colorBackground">@android:color/white</item>
    </style>

</resources>